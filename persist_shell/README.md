# Persist root shell
1. Obtain a root shell on the bgw320.
2. Verify that ``/var/etc/inetd.d/ssh`` exists, if it doesn't you can't use this persistence method
3. Run keepshell.sh
4. You know have a root shell that will persist through reboots and possibily updates